# Postgres. On Vercel this is injected by the Neon integration.
# Locally: a Neon branch, or `docker run -e POSTGRES_PASSWORD=dev -p 5432:5432 postgres`.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
